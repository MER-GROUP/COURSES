#!usr/bin/gnuplot --persist
# в первой строке указываем какая программа запускает скрипт

# рисунок в виде математических формул
# задаем правое вращение
a=a+1
zrot=(zrot+350)%360
# set view xrot,zrot
set view xrot, zrot
# начинаем рисовать 3D
splot x**2-y**2
# делаем пайзу в 2 сотых секунды
# скорость обновления
pause 0.1
# перерисовываем картинку
if (a<50) reread